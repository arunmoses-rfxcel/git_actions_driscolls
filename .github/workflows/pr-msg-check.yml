name: 'PR Title Check'

on:
  pull_request:
    types: [assigned, edited, opened, synchronize, reopened]

jobs:
  main:
    name: 'PR Title Check'
    runs-on: ubuntu-latest
    steps:
    - uses: rfxcel/gh-actions/pr-msg-check@master
      with:
        github_token: ${{ secrets.github_token }}

